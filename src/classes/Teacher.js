class Teacher {
    constructor(object) {
        // TODO: перепиcать логику построения объекта в зависимости от формата входных данных
        this.id = object.id
        this.image = object.image
        this.details = object.details
    }
}

export default Teacher;